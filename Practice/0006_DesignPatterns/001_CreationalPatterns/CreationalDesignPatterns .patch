Creational Design Patterns in C++
Creational design patterns deal with object creation mechanisms, increasing flexibility and reusability. These patterns provide a way to create objects dynamically while hiding complexity.

🔹 Types of Creational Design Patterns

->  Singleton → Ensures only one instance exists.

->  Factory Method → Creates objects dynamically based on input.

->  Abstract Factory → Creates families of related objects.

->  Builder → Builds complex objects step by step.

->  Prototype → Clones existing objects instead of creating new ones.